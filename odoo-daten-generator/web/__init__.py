"""Web layer for the Odoo demo data generator (S9).

Replaces the retired CustomTkinter wizard (`gui.py`). The pipeline itself
(`orchestrator.py` and `modules/*`) is untouched — S9 replaces the caller,
not the pipeline.
"""
